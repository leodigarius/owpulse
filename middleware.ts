// This middleware is disabled because it's not compatible with Next.js static export mode
// import { NextResponse } from 'next/server';
// import type { NextRequest } from 'next/server';

// Middleware disabled to support static export mode
export const config = {
  matcher: [],
}; 