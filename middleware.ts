// Middleware completely disabled to fix Vercel deployment issues
// import { NextResponse } from 'next/server';
// import type { NextRequest } from 'next/server';

// Empty matcher to ensure middleware doesn't run
export const config = {
  matcher: [],
}; 